name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, xml, ctype, iconv, intl, pdo_sqlite, dom, filter, gd, json, mbstring, pdo, pdo_mysql
          coverage: none

      - name: Install Composer dependencies
        run: composer install --optimize-autoloader --no-dev --prefer-dist

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install NPM dependencies
        run: npm ci

      - name: Build frontend assets
        run: npm run build

      - name: Deploy to server via SSH
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 208.109.173.77
          username: g0mbqxoz5uaj
          password: Timbuktu123@@@
          port: 22
          source: "app,bootstrap,config,database,public,resources,routes,storage,artisan,composer.json,composer.lock,package.json,package-lock.json,vite.config.js,tailwind.config.js,postcss.config.js"
          target: /home/g0mbqxoz5uaj/public_html/portal/
          strip_components: 0
          rm: false

      - name: Run post-deployment commands
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 208.109.173.77
          username: g0mbqxoz5uaj
          password: Timbuktu123@@@
          port: 22
          script: |
            cd /home/g0mbqxoz5uaj/public_html/portal
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache

